# Virtual env / caches
.venv/
__pycache__/
*.pyc
.DS_Store
.ipynb_checkpoints/

# Large raw inputs (regenerated by the pipeline) - NOT committed
data/raw/
data/interim/
*.7z

# Runtime demo artifact created by POST /data
data/processed/api_appended.csv

# Keep: data/processed/*.parquet, *.json, symbol_summary.csv, reports/  (small, needed by apps)

# Editor / environment-specific (not part of the project)
.claude/
.claude.backup*/
.vscode/
.idea/
