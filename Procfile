# Heroku / Render "native" (non-Docker) process for the FastAPI service.
# For the Streamlit UI use Streamlit Community Cloud or the Docker image.
web: uvicorn api:app --app-dir app --host 0.0.0.0 --port $PORT
